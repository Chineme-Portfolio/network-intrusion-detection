# Node (backend, frontend)
node_modules/
dist/
build/
*.tsbuildinfo

# Python (capture, ml-service)
__pycache__/
*.py[cod]

# Env / secrets — never commit real config (code-standards.md Section 6)
.env
.env.local

# Vite
.vite/

# Generated contract bindings (source of truth is shared/schemas/; regenerated by codegen)
shared/generated/
