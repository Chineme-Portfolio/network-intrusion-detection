# Caught ml-service — K1 dependencies (the keystone; spec 0002).
#
# The web layer (from Layer 0).
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.10,<3.0
# The pub/sub spine: subscribe `flows`, publish `verdicts` (redis-py async client).
redis>=5.2,<7.0
# The ML stack. scikit-learn MUST be pinned to 1.8.0 — the version the v1 models
# were trained and pickled under. Unpickling a joblib artifact under another sklearn
# version can warn, break, or silently shift behavior (library-docs.md, load-bearing).
scikit-learn==1.8.0
joblib>=1.3,<2.0
# The featurizer reuses the exact v1 02/02b transforms; a flow becomes the 67-feature frame.
# Range matches requirements-test.txt (the range the golden-fixture parity was proven on),
# so the serving featurizer behaves like the tested one; the parity guard catches any drift.
pandas>=2.2,<4.0
numpy>=1.26,<3.0
