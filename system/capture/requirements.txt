# Caught capture — Layer 0 dependencies.
#
# The idle skeleton needs nothing. The CSV-replay adapter (K1) adds redis-py
# (publish flow records) and pandas (read labeled CICIDS rows) — added then, not
# now (no scaffolding ahead, code-standards.md Section 1).
